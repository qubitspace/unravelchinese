
<div class="word <%=type%>" word-id="<%=id%>" type="show">

  <h3><%= simplified %></h3>

  <h4>Definitions:</h4>
  <%= concept("definition/definition_cell/definition_manage_cell", collection: definitions, current_user: current_user) %>

  <h4>Tags:</h4>
  <div class="word_tag">
    <% tags.each do |tag| %>
      • <%= tag.name %><br/>
    <% end %>
  </div>

  <h4>Actions:</h4>
  <% status_links.each do |link| %>
    <div class="link"><%= link %></div>
  <% end %>
</div>

