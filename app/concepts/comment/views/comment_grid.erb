
<%= concept( "comment/comment_cell", collection: comments, last: comments.last) %>

<%# unless comments.last_page? %>
  <div id='next'>
    <%#= link_to "More!", next_comments_article_path(id: model.id, page: comments.next_page), class: "button expand light", remote: true %>
  </div>
<%# end %>
