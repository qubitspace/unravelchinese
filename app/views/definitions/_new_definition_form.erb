<%= simple_form_for definition, url: word_create_definition_path(word), remote: true, method: :post do |f| %>
  <%= f.input :value %>
  <%= f.input :rank %>
  <%= f.button :submit, class: :expand %>
<% end %>

<%= definition.errors.messages %>

<%# TODO: make it ajax call %>
<%# do I need the method: post here %>