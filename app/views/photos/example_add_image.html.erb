<div class="add_section_form">
  <%= simple_form_for @add_photo_form, url: sections_path, remote: true, method: :post do |f| %>

    <%= f.input :article_id, :as => :hidden, :input_html => { :value => @article.id } %>
    <%= f.input :resource_id, collection: Photo.all %>
    <%= f.input :resource_type, :as => :hidden, :input_html => { :value => 'Photo' } %>

    <%= f.button :submit, class: :expand %>
  <% end %>
  <%= @add_photo_form.errors.messages unless @add_photo_form.errors.nil? %>
</div>